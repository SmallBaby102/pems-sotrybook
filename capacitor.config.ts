import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.jprdv-ddiserens.components',
  appName: 'JPRDV - David Diserens Components',
  webDir: 'build',
  bundledWebRuntime: false,

};

export default config;
